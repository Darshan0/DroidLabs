{
  "name": "droidlab-insights",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "cp index.source.html index.html && vite build",
    "build:pages-root": "npm run build && cp dist/index.html index.html && rm -rf assets && cp -R dist/assets assets",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^5.0.4",
    "vite": "^7.1.12",
    "typescript": "^5.9.3",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "lucide-react": "^0.468.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
